import { FaderDirective } from './fader.directive';

describe('FaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FaderDirective();
    expect(directive).toBeTruthy();
  });
});
